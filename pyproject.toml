[tool.poetry]
name = "mock-stock-game"
version = "0.1.0"
description = "Mock Stock Game"
authors = ["shhhappy"]

[tool.poetry.dependencies]
python = "^3.11"
flask = ">=3.0.0"
flask-sqlalchemy = ">=3.1.0"
werkzeug = ">=3.0.0"
gunicorn = ">=21.0.0"
openpyxl = ">=3.1.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
